// import express from "express";
// import { addPastQuestions, viewPastQuests, getSubjects } from "../controller/pastQuestController.js";
// import authenticateUser from "../middleware/authMiddleware.js";
// import multer from "multer";

// const router = express.Router();
// const upload = multer({ dest: 'uploads/' }); // Define the multer instance here

// // Use multer as middleware in the route
// router.post("/add-past-questions", authenticateUser, upload.single('file'), addPastQuestions);
// router.get("/pq/view-past-quest", viewPastQuests);
// router.get("/pq/subjects", getSubjects);

// export default router;
